name: Rebuild PR 1589 cleanly (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by the status-reporting rebuild workflow."
